import ForShopsClient from "./ForShopsClient";

export const metadata = {
  title: "TuneView for Shops — Give every customer a report card.",
  description:
    "Complete revision tracking, audit trails, and shop baseline libraries. Built for professional tuners.",
};

export default function ForShopsPage() {
  return <ForShopsClient />;
}
